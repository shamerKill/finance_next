import { ReactNode } from "react";

export const metadata = { title: "新建预测策略" };

export default function NewPredictionStrategyLayout({
  children,
}: {
  children: ReactNode;
}) {
  return <>{children}</>;
}
