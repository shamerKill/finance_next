import { ReactNode } from "react";

export const metadata = { title: "预测策略详情" };

export default function PredictionStrategyDetailLayout({
  children,
}: {
  children: ReactNode;
}) {
  return <>{children}</>;
}
